{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html src/styles.css src/app.js dist/",
    "dev": "python3 -m http.server 5173",
    "start": "node server.js"
  },
  "devDependencies": {}
}
